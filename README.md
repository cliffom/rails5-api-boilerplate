# Rails 5 API Boilerplate

A simple template with Docker files to get up and running with Rails 5

## Getting Started

```
docker-compose run --rm web bundle install
docker-compose run --rm web bundle exec rails c
```

